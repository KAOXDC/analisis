GIT 

git init   (crear un repositorio nuevo / vacio)
git add .  (grega al stage estado de preparacion del codigo)
git commit -m "DESCRIPCION DEL COMMIT"
gis status  (ver el estado del repositorio)
 


CMD

cd CARPETA  (ingrtesar a la carpeta)
cd ..     (regresa a la carpeta anterior)
dir      (muestra el contenido de una carpeta)
cls      (limpiar pantalla)

mkdir     (crear una carpeta)


